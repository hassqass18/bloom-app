-- Bloom — seed data (optional).
-- Identities/days/etc. are per-user and created at runtime after a user signs up,
-- so there is nothing global to seed here. This file is a placeholder for any
-- future reference data (e.g. default budget categories) that should ship with the DB.

-- Example (commented): default budget category suggestions could live in a
-- non-RLS reference table in a later migration.
