# Bloom app — pass these at run/build time via --dart-define (do NOT commit real values)
#
# flutter run \
#   --dart-define=SUPABASE_URL=https://<ref>.supabase.co \
#   --dart-define=SUPABASE_ANON_KEY=eyJ...
#
# Or use a dart-define-from-file JSON for convenience.
SUPABASE_URL=
SUPABASE_ANON_KEY=
